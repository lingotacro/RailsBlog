<h2>  Création d'un Article  </h2>

<%= form_for @post do |k| %>
  <div class="form-group">
    <%= k.text_field :title,  class: 'form-control', :required => true%>
  </div>
  <div class="form-group">
    <%= k.text_area :content,  class: 'form-control', :required => true%>
  </div>
  <div class="form-group">
    <button type="submit" class="btn btn-success"> Enregistrer </button>
  </div>
<% end %>